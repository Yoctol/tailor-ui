---
name: Radio
menu: Data Entry
route: /data-entry/radio
---

import { PropsTable, Playground } from 'docz';
import { Toggle, Value } from 'react-powerplug';

import Button from '../Button';
import Box from '../Grid/Box';

import Radio from './';
import RadioGroup from './RadioGroup';

# Radio

Radio.

## When To Use

- Used to select a single state in multiple options.
- The difference between Select is that Radio is visible to user and can facilitate the comparison of choice, which makes there shouldn't be too many of them.

## Examples

```js
import { Radio } from '@yoctol/ui';
```

### Radio

#### Basic

<Playground>
  <Radio>Radio</Radio>
  <br />
  <Radio defaultChecked>Radio</Radio>
</Playground>

#### Disabled

<Playground>
  <Radio checked disabled>Radio</Radio>
  <br />
  <Radio disabled>Disabled</Radio>
</Playground>

### Radio.Group

#### Basic

<Playground>
  <Value initial="radio-a">
    {({ set, value }) => (
      <Radio.Group
        options={[
          { label: 'Radio A', value: 'radio-a' },
          { label: 'Radio B', value: 'radio-b' },
          { label: 'Radio C', value: 'radio-c', disabled: true },
          { label: 'Radio D', value: 'radio-d' },
        ]}
        value={value}
        onChange={set}
      />
    )}
  </Value>
  <br />
  <Radio.Group
    options={[
      { label: 'Radio A', value: 'radio-a' },
      { label: 'Radio B', value: 'radio-b' },
      { label: 'Radio C', value: 'radio-c', disabled: true },
      { label: 'Radio D', value: 'radio-d' },
    ]}
    defaultValue="radio-b"
    onChange={console.log}
  />
</Playground>

#### Controlled

<Playground>
  <Value initial="radio-a">
    {({ set, value }) => (
      <div>
        <Radio.Group
          options={[
            { label: 'Radio A', value: 'radio-a' },
            { label: 'Radio B', value: 'radio-b' },
            { label: 'Radio C', value: 'radio-c', disabled: true },
            { label: 'Radio D', value: 'radio-d' },
          ]}
          value={value}
          onChange={set}
        />
        <br />
        <Button onClick={() => set('radio-d')}>Check last one</Button>
      </div>
    )}
  </Value>
</Playground>


#### Composition render

<Playground>
  <Value initial="radio_1">
    {({ set, value }) => (
      <Radio.Group value={value} onChange={set}>
        <Box bg="gray300" p="2" m="2">
          <Radio value="radio_1">radio 1</Radio>
        </Box>
        <Box bg="gray300" p="2" m="2">
          <Radio value="radio_2">radio 2</Radio>
        </Box>
        <Box bg="gray300" p="2" m="2">
          <Radio value="radio_3">radio 3</Radio>
        </Box>
        <Box bg="gray300" p="2" m="2">
          <Radio value="radio_4">radio 4</Radio>
        </Box>
      </Radio.Group>
    )}
  </Value>
  <br />
  <Radio.Group defaultValue="radio_1" onChange={console.log}>
    <Box bg="gray300" p="2" m="2">
      <Radio value="radio_1">radio 1</Radio>
    </Box>
    <Box bg="gray300" p="2" m="2">
      <Radio value="radio_2">radio 2</Radio>
    </Box>
    <Box bg="gray300" p="2" m="2">
      <Radio value="radio_3">radio 3</Radio>
    </Box>
    <Box bg="gray300" p="2" m="2">
      <Radio value="radio_4">radio 4</Radio>
    </Box>
  </Radio.Group>
</Playground>

#### Direction

<Playground>
  <Value initial="">
    {({ set, value }) => (
      <Radio.Group
        direction="vertical"
        options={[
          { label: 'radio 1', value: 'radio_1' },
          { label: 'radio 2', value: 'radio_2' },
          { label: 'radio 3', value: 'radio_3' },
          { label: 'radio 4', value: 'radio_4' },
        ]}
        value={value}
        onChange={set}
      />
    )}
  </Value>
</Playground>

## API

### Radio

<PropsTable of={Radio} />

### Radio.Group

<PropsTable of={RadioGroup} />

---
name: Tooltip
menu: Components
route: /components/tooltip
---

import { PropsTable, Playground } from 'docz';
import Tooltip from './';
import { Button, Space, Flex } from '../';

# Tooltip

A simple text popup tip.

## When To Use

- The tip is shown on mouse enter, and is hidden on mouse leave. The Tooltip doesn't support complex text or operations.
- To provide an explanation of a `button/text/operation`. It's often used instead of the html `title` attribute.

## Examples

```js
import { Tooltip } from '@yoctol/ui';
```

### Basic

<Playground>
  <Tooltip placement="top" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

### Click

<Playground>
  <Tooltip
    placement="top"
    trigger="click"
    content={<span>Tooltip Content</span>}
  >
    <Button>Button</Button>
  </Tooltip>
</Playground>

### Placement

<Playground>
  <Flex justifyContent="center">
    <Tooltip placement="topLeft" content={<span>Tooltip Content</span>}>
      <Button mr="2">Top Left</Button>
    </Tooltip>
    <Tooltip placement="top" content={<span>Tooltip Content</span>}>
      <Button mr="2">Top</Button>
    </Tooltip>
    <Tooltip placement="topRight" content={<span>Tooltip Content</span>}>
      <Button>Top Right</Button>
    </Tooltip>
  </Flex>

  <Flex>
    <Flex flexDirection="column">
      <Tooltip placement="leftTop" content={<span>Tooltip Content</span>}>
        <Button mb="2">Left Top</Button>
      </Tooltip>
      <Tooltip placement="left" content={<span>Tooltip Content</span>}>
        <Button mb="2">Left</Button>
      </Tooltip>
      <Tooltip placement="leftBottom" content={<span>Tooltip Content</span>}>
        <Button>Left Bottom</Button>
      </Tooltip>
    </Flex>

    <Flex ml="auto" flexDirection="column">
      <Tooltip placement="rightTop" content={<span>Tooltip Content</span>}>
        <Button mb="2">Right Top</Button>
      </Tooltip>
      <Tooltip placement="right" content={<span>Tooltip Content</span>}>
        <Button mb="2">Right</Button>
      </Tooltip>
      <Tooltip placement="rightBottom" content={<span>Tooltip Content</span>}>
        <Button>Right Bottom</Button>
      </Tooltip>
    </Flex>

  </Flex>

  <Flex justifyContent="center">
    <Tooltip placement="bottomLeft" content={<span>Tooltip Content</span>}>
      <Button mr="2" >Bottom Left</Button>
    </Tooltip>
    <Tooltip placement="bottom" content={<span>Tooltip Content</span>}>
      <Button mr="2">Bottom</Button>
    </Tooltip>
    <Tooltip placement="bottomRight" content={<span>Tooltip Content</span>}>
      <Button>Bottom Right</Button>
    </Tooltip>
  </Flex>
</Playground>

### Light

<Playground>
  <Tooltip light placement="top" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

### With content hide

<Playground>
  <Tooltip
    light
    placement="right"
    trigger="click"
    content={hide => (
      <div>
        Tooltip Content
        <Button size="sm" onClick={hide}>
          Hide Tooltip
        </Button>
      </div>
    )}
  >
    <Button>Button</Button>
  </Tooltip>
</Playground>

### With onVisibleChange

<Playground>
  <Tooltip
    placement="right"
    trigger="click"
    content="Tooltip content"
    onVisibleChange={console.log}
  >
    <Button>Button</Button>
  </Tooltip>
  <br />
  <br />
  <Tooltip
    placement="right"
    content="Tooltip content"
    onVisibleChange={console.log}
  >
    <Button>Button</Button>
  </Tooltip>
</Playground>

### With defaultVisible

<Playground>
  <Tooltip
    placement="right"
    trigger="click"
    content="Tooltip content"
    defaultVisible
  >
    <Button>Button</Button>
  </Tooltip>
  <br />
  <br />
  <Tooltip placement="right" content="Tooltip content" defaultVisible>
    <Button>Button</Button>
  </Tooltip>
</Playground>

## API

<PropsTable of={Tooltip} />

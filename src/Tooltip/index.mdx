---
name: Tooltip
menu: Data Display
route: /data-display/tooltip
---

import { PropsTable, Playground } from 'docz';
import Tooltip from './';
import { Button } from '../';

# Tooltip

A simple text popup tip.

## When To Use

- The tip is shown on mouse enter, and is hidden on mouse leave. The Tooltip doesn't support complex text or operations.
- To provide an explanation of a `button/text/operation`. It's often used instead of the html `title` attribute.

## Examples

```js
import { Tooltip } from '@yoctol/ui';
```

### Basic

#### Top & Hover

<Playground>
  <Tooltip placement="top" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

#### Right & Hover

<Playground>
  <Tooltip placement="right" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

#### Left & Click

<Playground>
  <Tooltip placement="left" trigger="click" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

#### Bottom & Click

<Playground>
  <Tooltip placement="bottom" trigger="click" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

### Light

#### Top & Hover

<Playground>
  <Tooltip light placement="top" content={<span>Tooltip Content</span>}>
    <Button>Button</Button>
  </Tooltip>
</Playground>

### Right & Click

<Playground>
  <Tooltip light placement="right" trigger="click" content={<span>Tooltip Content</span>}>
    <Button onClick={() => console.log('clicked')}>Button</Button>
  </Tooltip>
</Playground>

### With content hide

<Playground>
  <Tooltip
    light
    placement="right"
    trigger="click"
    content={hide => (
      <div>
        Tooltip Content
        <Button size="sm" onClick={hide}>Hide Tooltip</Button>
      </div>
    )}
  >
    <Button>Button</Button>
  </Tooltip>
</Playground>

### With onVisibleChange

<Playground>
  <Tooltip
    placement="right"
    trigger="click"
    content="Tooltip content"
    onVisibleChange={console.log}
  >
    <Button>Button</Button>
  </Tooltip>
  <br />
  <br />
  <Tooltip
    placement="right"
    content="Tooltip content"
    onVisibleChange={console.log}
  >
    <Button>Button</Button>
  </Tooltip>
</Playground>

## API

<PropsTable of={Tooltip} />

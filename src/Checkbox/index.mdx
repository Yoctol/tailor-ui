---
name: Checkbox
menu: Data Entry
route: /data-entry/checkbox
---

import { PropsTable, Playground } from 'docz';
import { Toggle, Value } from 'react-powerplug';

import FormField from '../Form/FormField';
import Label from '../Form/Label';
import Button from '../Button';

import Checkbox from '.';
import CheckboxGroup from './CheckboxGroup';

# Checkbox

Checkbox component.

## When To Use

- Used for selecting multiple values from several options.
- If you use only one checkbox, it is the same as using Switch to toggle between two states. The difference is that Switch will trigger the state change directly, but Checkbox just marks the state as changed and this needs to be submitted.

## Examples

```js
import { FormField, Label, Checkbox, Checkbox } from '@yoctol/ui';
```

### Checkbox

#### Basic

<Playground>
  <Checkbox defaultChecked>Uncontrolled Checkbox</Checkbox>
  <br />
  <Toggle>
    {({ on, toggle }) => (
      <Checkbox
        checked={on}
        onChange={toggle}
      >
        Controlled Checkbox
      </Checkbox>
    )}
  </Toggle>
</Playground>

#### With disabled

<Playground>
  <Checkbox checked disabled>Checked Disabled</Checkbox>
  <Checkbox disabled>Disabled</Checkbox>
</Playground>

### Checkbox.Group

#### Basic

<Playground>
  <Value initial={['check_1']}>
    {({ set, value }) => (
      <Checkbox.Group
        label="Checkbox Group"
        options={[
          { label: 'checkbox 1', value: 'check_1' },
          { label: 'checkbox 2', value: 'check_2' },
          { label: 'checkbox 3', value: 'check_3', disabled: true },
          { label: 'checkbox 4', value: 'check_4' },
        ]}
        values={value}
        onChange={set}
      />
    )}
  </Value>
</Playground>

#### With group & error

<Playground>
  <Value initial={[]}>
    {({ set, value }) => (
      <Checkbox.Group
        label="Checkbox Group"
        error
        message="Something went wrong!"
        options={[
          { label: 'checkbox 1', value: 'check_1' },
          { label: 'checkbox 2', value: 'check_2' },
          { label: 'checkbox 3', value: 'check_3' },
          { label: 'checkbox 4', value: 'check_4' },
        ]}
        values={value}
        onChange={val => set(val)}
      />
    )}
  </Value>
</Playground>

#### With controlled

<Playground>
  <Value initial={[]}>
    {({ set, value }) => (
      <div>
        <Checkbox.Group
          label="Checkbox Group"
          options={[
            { label: 'checkbox 1', value: 'check_1' },
            { label: 'checkbox 2', value: 'check_2' },
            { label: 'checkbox 3', value: 'check_3' },
            { label: 'checkbox 4', value: 'check_4' },
          ]}
          values={value}
          onChange={set}
        />
        <br />
        <Button onClick={() => set(['check_1', 'check_2', 'check_3', 'check_4'])}>Select All</Button>
        <Button ml="2" onClick={() => set([])}>Unselect All</Button>
      </div>
    )}
  </Value>
</Playground>

#### With direction

<Playground>
  <Value initial={[]}>
    {({ set, value }) => (
      <Checkbox.Group
        label="Checkbox Group"
        direction="vertical"
        options={[
          { label: 'checkbox 1', value: 'check_1' },
          { label: 'checkbox 2', value: 'check_2' },
          { label: 'checkbox 3', value: 'check_3' },
          { label: 'checkbox 4', value: 'check_4' },
        ]}
        values={value}
        onChange={set}
      />
    )}
  </Value>
</Playground>

## API

### Checkbox

<PropsTable of={Checkbox} />

### Checkbox.Group

<PropsTable of={CheckboxGroup} />

---
name: Checkbox
menu: Data Entry
route: /data-entry/checkbox
---

import { PropsTable, Playground } from 'docz';
import { Toggle, Value } from 'react-powerplug';
import FormField from '../Form/FormField';
import Label from '../Form/Label';
import Button from '../Button';
import Checkbox from '.';
import CheckboxField, { CheckboxFieldGroup } from './CheckboxField';


# Checkbox

Checkbox component.

## When To Use

- Used for selecting multiple values from several options.
- If you use only one checkbox, it is the same as using Switch to toggle between two states. The difference is that Switch will trigger the state change directly, but Checkbox just marks the state as changed and this needs to be submitted.

## Examples

```js
import { FormField, Label, Checkbox, CheckboxField } from '@yoctol/ui';
```

### Checkbox

#### Basic

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <FormField>
        <Checkbox
          id="checkbox-basic"
          checked={on}
          onChange={toggle}
        />
        <Label htmlFor="checkbox-basic">Checkbox</Label>
      </FormField>
    )}
  </Toggle>
</Playground>

#### With disabled

<Playground>
  <FormField>
    <Checkbox id="checked-disabled" checked disabled />
    <Label htmlFor="checked-disabled">Checked Disabled</Label>
  </FormField>
  <FormField>
    <Checkbox id="disabled" disabled />
    <Label htmlFor="disabled">Disabled</Label>
  </FormField>
</Playground>

#### Customize color

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <FormField>
        <Checkbox
          id="checkbox-custom"
          bg="#63bf2d"
          borderColor="#423b63"
          checked={on}
          onChange={toggle}
        />
        <Label htmlFor="checkbox-custom">Checkbox</Label>
      </FormField>
    )}
  </Toggle>
</Playground>

### CheckboxField

#### Basic

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <CheckboxField checked={on} onChange={toggle}>
        Checkbox
      </CheckboxField>
    )}
  </Toggle>
</Playground>

#### With error

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <CheckboxField
        checked={on}
        onChange={toggle}
        error
        message="Something went wrong!"
      >
        Checkbox
      </CheckboxField>
    )}
  </Toggle>
</Playground>

### CheckboxField.Group

#### Basic

<Playground>
  <Value initial={['check_1']}>
    {({ set, value }) => (
      <CheckboxField.Group
        label="Checkbox Group"
        options={[
          { label: 'checkbox 1', value: 'check_1' },
          { label: 'checkbox 2', value: 'check_2' },
          { label: 'checkbox 3', value: 'check_3', disabled: true },
          { label: 'checkbox 4', value: 'check_4' },
        ]}
        values={value}
        onChange={set}
      />
    )}
  </Value>
</Playground>

#### With group & error

<Playground>
  <Value initial={[]}>
    {({ set, value }) => (
      <CheckboxField.Group
        label="Checkbox Group"
        error
        message="Something went wrong!"
        options={[
          { label: 'checkbox 1', value: 'check_1' },
          { label: 'checkbox 2', value: 'check_2' },
          { label: 'checkbox 3', value: 'check_3' },
          { label: 'checkbox 4', value: 'check_4' },
        ]}
        values={value}
        onChange={val => set(val)}
      />
    )}
  </Value>
</Playground>

#### With controlled

<Playground>
  <Value initial={[]}>
    {({ set, value }) => (
      <div>
        <CheckboxField.Group
          label="Checkbox Group"
          options={[
            { label: 'checkbox 1', value: 'check_1' },
            { label: 'checkbox 2', value: 'check_2' },
            { label: 'checkbox 3', value: 'check_3' },
            { label: 'checkbox 4', value: 'check_4' },
          ]}
          values={value}
          onChange={set}
        />
        <Button onClick={() => set(['check_1', 'check_2', 'check_3', 'check_4'])}>Select All</Button>
        <Button ml="2" onClick={() => set([])}>Unselect All</Button>
      </div>
    )}
  </Value>
</Playground>


## API

### Checkbox

<PropsTable of={Checkbox} />

### CheckboxField

<PropsTable of={CheckboxField} />

### CheckboxField.Group

<PropsTable of={CheckboxFieldGroup} />

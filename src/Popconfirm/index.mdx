---
name: Popconfirm
menu: Components
route: /components/popconfirm
---

import { PropsTable, Playground, Link } from 'docz';

import { Button, Modal, message } from '../';

import Popconfirm from './';

# Popconfirm

A simple and compact confirmation dialog of an action.

## When To Use

A simple and compact dialog used for asking for user confirmation.

## Examples

```js
import { Popconfirm } from '@yoctol/ui';
```

### Basic

<Playground>
  <Popconfirm
    placement="right"
    content="Are you sure to delete this todo?"
    onConfirm={() => console.log('confirm')}
    onCancel={() => console.log('cancel')}
  >
    <Button>Button</Button>
  </Popconfirm>
</Playground>

### With customize text & type

<Playground>
  <Popconfirm
    placement="right"
    type="error"
    confirmText="毀了它"
    cancelText="一小時後再提醒我"
    content="此操作將會毀滅本系統，你確定？"
    onConfirm={() => {
      console.log('confirm');
      message.success('您的系統已被毀滅');
    }}
    onCancel={() => {
      Modal.success({
        title: '我才不會提醒你',
        content: '您的系統已被毀滅',
        confirmText: '感謝',
      });
      console.log('cancel');
    }}
  >
    <Button>Button</Button>
  </Popconfirm>
</Playground>

## API

<PropsTable of={Popconfirm} />

<div>
  Consult{' '}
  <Link to="/data-display/tooltip#properties"> Tooltip's documentation</Link> to
  find more APIs.
</div>

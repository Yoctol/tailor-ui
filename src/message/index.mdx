---
name: Message
menu: Feedback
route: /feedback/message
---

import { PropsTable, Playground } from 'docz';
import message from './';
import Button from '../Button';

# Message

## API

### Methods

- `message.info(content, [duration]) => Promise`
- `message.success(content, [duration]) => Promise`
- `message.warning(content, [duration]) => Promise`
- `message.error(content, [duration]) => Promise`

### Argument

| Argument | Description                   | Type                | Default |
| -------- | ----------------------------- | ------------------- | ------- |
| content  | content of the message        | string \| ReactNode | -       |
| duration | time(ms) before auto-dismiss. | number              | 3000    |


## Usage

```js
import { message } from '@yoctol/ui'
```

### Basic

<Playground>
  <Button onClick={() => message.info('This is a info message!')}>
    Info
  </Button>

  <Button onClick={() => message.success('This is a success message!')} ml={2}>
    Success
  </Button>

  <Button onClick={() => message.warning('This is a warning message!')} ml={2}>
    Warning
  </Button>

  <Button onClick={() => message.error('This is a error message!')} ml={2}>
    Error
  </Button>
</Playground>

### Customize duration

<Playground>
  <Button
    onClick={() => {
      message.info(
        'This is a prompt message for success, and it will disappear in 10 seconds',
        10000
      );
    }}
  >
    Customized display duration
  </Button>
</Playground>

### Handle Promise

<Playground>
  <Button onClick={async () => {
    await message.info('This is a info message!');
    message.success('Previous message has been resolve!');
  }}>
    Show a message
  </Button>
</Playground>

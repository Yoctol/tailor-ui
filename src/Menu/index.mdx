---
name: Menu
menu: Navigation
route: /navigation/menu
---

import { PropsTable, Playground } from 'docz';
import { Value } from 'react-powerplug';
import { range } from 'ramda';

import { Box } from '../';

import Menu from './';
import Item from './Item';
import SubMenu from './SubMenu';


# Menu

Menu list of Navigation.

## When To Use

Navigation menu is important for a website, it helps users jump from one site section to another quickly. Mostly, it includes top navigation and side navigation. Top navigation provides all the category and functions of the website. Side navigation provides the Multi-level structure of the website.

## Examples

```js
import { Menu } from '@yoctol/ui';
```

### Basic

<Playground>
  <Value initial="1">
    {({ value, set }) => (
      <Box height="600px">
        <Menu onChange={console.log} initial="1">
          {range(1, 10).map(key => (
            <Menu.Item key={key} active={value === key} onClick={() => set(key)}>
              Menu {key}
            </Menu.Item>
          ))}
        </Menu>
      </Box>
    )}
  </Value>
</Playground>

### Group with custom width

<Playground>
  <Value initial="1">
    {({ value, set }) => (
      <Menu>
        <Menu.SubMenu title="Group 1" icon="understood" initial width={180}>
          <Menu.Item active={value === '1'} onClick={() => set('1')}>
            Item 1
          </Menu.Item>
          <Menu.Item active={value === '2'} onClick={() => set('2')}>
            Item 2
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu title="Group 2" icon="kurator">
          <Menu.Item active={value === '3'} onClick={() => set('3')}>
            Item 3
          </Menu.Item>
          <Menu.Item active={value === '4'} onClick={() => set('4')}>
            Item 4
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu title="Group 3" icon="touch">
          <Menu.Item active={value === '5'} onClick={() => set('5')}>
            Item 5
          </Menu.Item>
          <Menu.Item active={value === '6'} onClick={() => set('6')}>
            Item 6
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.Item
          icon="tags"
          active={value === '7'}
          onClick={() => set('7')}
        >
          Item 7
        </Menu.Item>
        <Menu.Item
          icon="settings"
          active={value === '8'}
          onClick={() => set('8')}
        >
          Item 8
        </Menu.Item>
      </Menu>
    )}
  </Value>
</Playground>

### Disable togglable

<Playground>
  <Value initial="1">
    {({ value, set }) => (
      <Menu>
        <Menu.SubMenu
          title="Understood"
          icon="understood"
          togglable={false}
          onClick={() => console.log('click understood')}
        >
          <Menu.Item active={value === 'intent'} onClick={() => set('intent')}>
            Intent
          </Menu.Item>
          <Menu.Item active={value === 'entity'} onClick={() => set('entity')}>
            Entity
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu title="Kurator" icon="kurator">
          <Menu.Item active={value === 'action'} onClick={() => set('action')}>
            Action
          </Menu.Item>
          <Menu.Item active={value === 'menu'} onClick={() => set('menu')}>
            Menu
          </Menu.Item>
        </Menu.SubMenu>
      </Menu>
    )}
  </Value>
</Playground>

## API

### Menu
<PropsTable of={Menu} />

### Menu.SubMenu
<PropsTable of={SubMenu} />

### Menu.Item
<PropsTable of={Item} />

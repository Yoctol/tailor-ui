import { Button } from 'tailor-ui';
import theme from 'docs/theme.js';
import { MdArrowForward, MdCheck } from 'react-icons/md';
import { LiveEdit, CodeBlock, GoBackButton } from 'docs/src/modules/components';

<GoBackButton />

# Button

### To trigger an operation.

<CodeBlock
  code="import { Button } from 'tailor-ui';"
  language="javascript"
  theme={theme}
/>

## When To Use

A button means an operation (or a series of operations). Clicking a button will trigger corresponding business logic.

## Basic Buttons

<LiveEdit
  scope={{ Button }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button>Basic</Button>
  <Button rounded ml="2">
    Basic
  </Button>
  <Button disabled ml="2">
    Basic
  </Button>
  <Button loading ml="2">
    Basic
  </Button>
</div>
`}
></LiveEdit>

### Variant

<LiveEdit
  scope={{ Button }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button variant="primary" ml="2">
    Primary
  </Button>
  <Button variant="danger" ml="2">
    Danger
  </Button>
  <Button variant="regular" ml="2">
    Regular
  </Button>
  <Button variant="normal" ml="2">
    Normal
  </Button>
  <br />
  <br />
  <Button variant="primary-invert" ml="2">
    Primary
  </Button>
  <Button variant="danger-invert" ml="2">
    Danger
  </Button>
  <Button variant="regular-invert" ml="2">
    Regular
  </Button>
  <Button variant="normal-invert" ml="2">
    Normal
  </Button>
</div>
`}
></LiveEdit>

### Rounded

<LiveEdit
  scope={{ Button }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button rounded variant="primary" ml="2">
    Primary
  </Button>
  <Button rounded variant="danger" ml="2">
    Danger
  </Button>
  <Button rounded variant="regular" ml="2">
    Regular
  </Button>
  <Button rounded variant="normal" ml="2">
    Normal
  </Button>
  <br />
  <br />
  <Button rounded variant="primary-invert" ml="2">
    Primary
  </Button>
  <Button rounded variant="danger-invert" ml="2">
    Danger
  </Button>
  <Button rounded variant="regular-invert" ml="2">
    Regular
  </Button>
  <Button rounded variant="normal-invert" ml="2">
    Normal
  </Button>
</div>
`}
></LiveEdit>

### Icon Buttons

Icon With Text Buttons

<CodeBlock
  code="import { MdArrowForward } from 'react-icons/md';"
  theme={theme}
  language="javascript"
/>

<LiveEdit
  scope={{ Button, MdArrowForward }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button icon={MdArrowForward} variant="primary" ml="2">
    Primary
  </Button>
  <Button icon={MdArrowForward} variant="danger" ml="2">
    Danger
  </Button>
  <Button icon={MdArrowForward} variant="regular" ml="2">
    Regular
  </Button>
  <Button icon={MdArrowForward} variant="normal" ml="2">
    Normal
  </Button>
  <br />
  <br />
  <Button icon={MdArrowForward} variant="primary-invert" ml="2">
    Primary
  </Button>
  <Button icon={MdArrowForward} variant="danger-invert" ml="2">
    Danger
  </Button>
  <Button icon={MdArrowForward} variant="regular-invert" ml="2">
    Regular
  </Button>
  <Button icon={MdArrowForward} variant="normal-invert" ml="2">
    Normal
  </Button>
</div>
`}
></LiveEdit>

Icon Only Buttons

<CodeBlock
  code="import { MdCheck } from 'react-icons/md';"
  language="javascript"
  theme={theme}
/>

<LiveEdit
  scope={{ Button, MdCheck }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button icon={MdCheck} variant="primary" rounded />
  <Button icon={MdCheck} variant="primary-invert" rounded ml="2" />
  <Button icon={MdCheck} variant="danger" rounded ml="2" />
  <Button icon={MdCheck} variant="danger-invert" rounded ml="2" />
  <Button icon={MdCheck} variant="regular" rounded ml="2" />
  <Button icon={MdCheck} variant="normal" rounded ml="2" />
</div>
`}
></LiveEdit>

### Disabled

<LiveEdit
  scope={{ Button }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button disabled variant="primary" ml="2">
    Primary
  </Button>
  <Button disabled variant="danger" ml="2">
    Danger
  </Button>
  <Button disabled variant="regular" ml="2">
    Regular
  </Button>
  <Button disabled variant="normal" ml="2">
    Normal
  </Button>
</div>
`}
></LiveEdit>

### Loading

<LiveEdit
  scope={{ Button }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Button loading variant="primary" ml="2">
    Primary
  </Button>
  <Button loading variant="danger" ml="2">
    Danger
  </Button>
  <Button loading variant="normal" ml="2">
    Normal
  </Button>
  <Button loading variant="regular" ml="2">
    Regular
  </Button>
  <br />
  <br />
  <Button loading variant="primary-invert" ml="2">
    Primary
  </Button>
  <Button loading variant="danger-invert" ml="2">
    Danger
  </Button>
  <Button loading variant="normal-invert" ml="2">
    Normal
  </Button>
  <Button loading variant="regular-invert" ml="2">
    Regular
  </Button>
</div>
`}
></LiveEdit>

### Stated Button Example

<LiveEdit
  scope={{ Button }}
  theme={theme}
  language={'jsx'}
  code={`
() => {
  const [loading, setLoading] = React.useState(false);
  return (
    <Button
      loading={loading}
      onClick={() => {
        setLoading(true);
        setTimeout(() => setLoading(false), 3000);
      }}
    >
      Click me!
    </Button>
  );
}
`}
></LiveEdit>

### Sizes

<LiveEdit scope={{ Button }} theme={theme} language="jsx" code={`
<div>
  <Button size="sm">
    Button sm
  </Button>

<Button ml={2}>Button md</Button>

<Button ml={2} size="lg">
  Button lg
</Button>

</div>
`}>
</LiveEdit>

## API

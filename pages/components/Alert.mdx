import { Alert } from 'tailor-ui';
import theme from 'docs/theme.js';
import { LiveEdit, CodeBlock, GoBackButton } from 'docs/src/modules/components';

<GoBackButton />

# Alert

### Alert component used as feedback.

<CodeBlock
  code="import { Alert } from 'tailor-ui';"
  language="javascript"
  theme={theme}
/>

## When To Use

- When you need to show alert messages to users.
- When you need a persistent static container which is closable by user actions.

### Basic Alerts

<LiveEdit
  scope={{ Alert }}
  theme={theme}
  language="jsx"
  code={`<Alert message="test" />`}
></LiveEdit>

### With Type

<LiveEdit
  scope={{ Alert }}
  theme={theme}
  language="jsx"
  code={`
<div>
  <Alert message="Info Text" type="info" />
  <Alert message="Success Text" type="success" />
  <Alert message="Warning Text" type="warning" />
  <Alert message="Error Text" type="error" />
</div>`}
></LiveEdit>

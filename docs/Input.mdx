---
name: Input
menu: Components
route: /components/input
---

import { PropsTable, Playground } from 'docz';
import { Button, Textarea, FormField, Icon, Space, Input } from 'tailor-ui';
import { MdAccountCircle } from 'react-icons/md';

# Input

A basic widget for getting the user input is a text field. Keyboard and mouse can be used for providing or changing data.

## When To Use

- A user input in a form field is needed.
- A search input is required.

## Examples

```js
import { Input, Textarea, FormField } from 'tailor-ui';
```

### Input

#### Basic

<Playground>
  <Input placeholder="placeholder" />
</Playground>

#### AutoSelect

<Playground>
  <Input
    autoSelect
    defaultValue="Example default value"
    placeholder="placeholder"
  />
</Playground>

#### With FormField & Label

<Playground>
  <FormField>
    <Label htmlFor="demo">With Default:</Label>
    <Input id="demo" placeholder="placeholder" />
  </FormField>
</Playground>

#### With errors

<Playground>
  <FormField label="With Error" validationMessage="Something went wrong!">
    <Input placeholder="none status" />
  </FormField>
</Playground>

#### With disabled

<Playground>
  <Label htmlFor="demo">With Disabled:</Label>
  <Input id="demo" placeholder="placeholder" disabled />
</Playground>

#### With size

<Playground>
  <FormField>
    <Label htmlFor="demo_sm">With sm:</Label>
    <Input id="demo_sm" size="sm" placeholder="placeholder" />
  </FormField>
  <FormField>
    <Label htmlFor="demo_md">With md:</Label>
    <Input id="demo_md" size="md" placeholder="placeholder" />
  </FormField>
  <FormField>
    <Label htmlFor="demo_lg">With lg:</Label>
    <Input id="demo_lg" size="lg" placeholder="placeholder" />
  </FormField>
</Playground>

### Prefix an Suffix

<Playground>
  <Input prefix="Prefix" defaultValue="" />

<Space mt="2">
  <Input suffix="Suffix" defaultValue="" />
</Space>

<Space mt="2">
  <Input suffix={<Button type="success">Send It</Button>} defaultValue="" />
</Space>

<Space mt="2">
  <Input prefix="https://" suffix=".com" defaultValue="yoctol" />
</Space>

  <Space mt="2">
    <Input
      prefix={
        <>
          <Icon type={MdAccountCircle} size="20" mr="1"/>
          username:
        </>
      }
      defaultValue="yoctol"
    />
  </Space>
</Playground>

#### focus and blur with ref

<Playground>
  {() => {
    const ref = React.useRef();

    return (
      <>
        <Input
          ref={ref}
          onPressEnter={() => {
            ref.current.blur();
          }}
        />
        <Button mt="2" onClick={() => ref.current.focus()}>Focus</Button>
      </>
    )

}}

</Playground>

### Textarea

#### Basic

<Playground>
  <FormField>
    <Label htmlFor="demo">Basic Textarea</Label>
    <Textarea id="demo" placeholder="placeholder" />
  </FormField>
</Playground>

#### With rows & maxRows

<Playground>
  <FormField>
    <Label htmlFor="demo">With rows & maxRows:</Label>
    <Textarea id="demo" placeholder="placeholder" rows={3} maxRows={6} />
  </FormField>
</Playground>

## API

### Input

<PropsTable of={Input} />

### Textarea

<PropsTable of={Textarea} />

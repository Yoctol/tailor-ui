---
name: RadioField
menu: Formik
route: /formik/radio-field
---

import { PropsTable, Playground } from 'docz';
import { RadioField } from '@tailor-ui/formik';
import { Button } from 'tailor-ui';
import { Formik } from 'formik';
import { object, string } from 'yup';

# RadioField

## Usage

<Playground>
  <Formik
    initialValues={{
      option: '',
    }}
    validationSchema={object().shape({
      option: string().required(),
    })}
    onSubmit={values => {
      console.log(JSON.stringify(values, null, 2));
    }}
  >
    {({ handleSubmit }) => (
      <form onSubmit={handleSubmit}>
        <RadioField
          label="Option"
          name="option"
          required
          options={[
            { label: 'Radio A', value: 'radio-a' },
            { label: 'Radio B', value: 'radio-b' },
            { label: 'Radio C', value: 'radio-c', disabled: true },
            { label: 'Radio D', value: 'radio-d' },
          ]}
        />
        <Button type="submit">Submit</Button>
      </form>
    )}
  </Formik>
</Playground>

## API

<PropsTable of={RadioField} />

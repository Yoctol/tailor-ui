---
name: TextareaField
menu: Formik
route: /formik/textarea-field
---

import { PropsTable, Playground } from 'docz';
import { TextareaField } from '@tailor-ui/formik';
import { Button } from 'tailor-ui';
import { Formik } from 'formik';
import { object, string } from 'yup';

# TextareaField

## Usage

<Playground>
  <Formik
    initialValues={{
      description: '',
    }}
    validationSchema={object().shape({
      description: string().required(),
    })}
    onSubmit={values => {
      console.log(JSON.stringify(values, null, 2));
    }}
  >
    {({ handleSubmit }) => (
      <form onSubmit={handleSubmit}>
        <TextareaField label="Description" name="description" required autoComplete="off" />
        <Button type="submit">Submit</Button>
      </form>
    )}
  </Formik>
</Playground>

## API

<PropsTable of={TextareaField} />

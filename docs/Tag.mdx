---
name: Tag
menu: Components
route: /components/tag
---

import { PropsTable, Playground } from 'docz';
import { Input, Button, Flex, Box, Tag } from 'tailor-ui';

# Tag

Tag for categorizing or markup.

## When To Use

- It can be used to tag by dimension or property.
- When categorizing.

## Examples

```js
import { Tag } from 'tailor-ui';
```

### Basic

<Playground>
  <Tag>Content</Tag>
  <Tag>Content</Tag>
</Playground>

### Closable

<Playground>
  <Tag closable>Content</Tag>
  <Tag closable>Content</Tag>
  <Tag closable>Content</Tag>
  <Tag closable>Content</Tag>
</Playground>

### Add & Remove Dynamically

<Playground>
  {() => {
    const [values, setValues] = React.useState(['tag 1', 'tag 2', 'tag 3']);
    const [editing, setEditing] = React.useState(false);

    const handleCreateTag = event => {
      const value = event.target.value.trim();

      if (value !== '') {
        setValues([...values, value]);
      }

      setEditing(false);
    };

    return (
      <Flex>
        {values.map((value, index) => (
          <Tag
            key={value}
            closable={index !== 0}
            onClosed={() => setValues(values.filter(v => v !== value))}
          >
            {value}
          </Tag>
        ))}
        <Flex ml="2">
          {editing ? (
            <Input
              width="80px"
              size="sm"
              defaultValue=""
              autoFocus
              onBlur={handleCreateTag}
              onPressEnter={handleCreateTag}
            />
          ) : (
            <Button text size="sm" onClick={() => setEditing(true)}>
              + Add Tag
            </Button>
          )}
        </Flex>
      </Flex>
    );

}}

</Playground>

## API

<!-- FIXME: API Table -->
<!-- <PropsTable of={Tag} /> -->

---
name: Menu
menu: Components
route: /components/menu
---

import { PropsTable, Playground } from 'docz';
import { range } from 'ramda';
import { Menu, Box } from 'tailor-ui';

# Menu

Menu list of Navigation.

## When To Use

Navigation menu is important for a website, it helps users jump from one site section to another quickly. Mostly, it includes top navigation and side navigation. Top navigation provides all the category and functions of the website. Side navigation provides the Multi-level structure of the website.

## Examples

```js
import { Menu } from 'tailor-ui';
```

### Basic

<Playground>
  {() => {
    const [value, setValue] = React.useState('1');

    return (
      <Box height="600px">
        <Menu onChange={console.log}>
          {range(1, 10).map(key => (
            <Menu.Item
              key={key}
              active={value === key}
              onClick={() => setValue(key)}
            >
              Menu {key}
            </Menu.Item>
          ))}
        </Menu>
      </Box>
    );

}}

</Playground>

### Group

<Playground>
  {() => {
    const [value, setValue] = React.useState('1');

    return (
      <Menu>
        <Menu.SubMenu id="understood" title="Group 1" icon="understood">
          <Menu.Item active={value === '1'} onClick={() => setValue('1')}>
            Item 1
          </Menu.Item>
          <Menu.Item active={value === '2'} onClick={() => setValue('2')}>
            Item 2
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu id="kurator" title="Group 2" icon="kurator">
          <Menu.Item active={value === '3'} onClick={() => setValue('3')}>
            Item 3
          </Menu.Item>
          <Menu.Item active={value === '4'} onClick={() => setValue('4')}>
            Item 4
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu id="touch" title="Group 3" icon="touch">
          <Menu.Item active={value === '5'} onClick={() => setValue('5')}>
            Item 5
          </Menu.Item>
          <Menu.Item active={value === '6'} onClick={() => setValue('6')}>
            Item 6
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.Item id="tags" icon="tags" active={value === '7'} onClick={() => setValue('7')}>
          Item 7
        </Menu.Item>
        <Menu.Item
          icon="settings"
          active={value === '8'}
          onClick={() => setValue('8')}
        >
          Item 8
        </Menu.Item>
      </Menu>
    );

}}

</Playground>

### With current sub only

<Playground>
  {() => {
    const [value, setValue] = React.useState('1');

    return (
      <Menu currentSubOnly defaultSubKeys={['understood']}>
        <Menu.SubMenu id="understood" title="Group 1" icon="understood">
          <Menu.Item active={value === '1'} onClick={() => setValue('1')}>
            Item 1
          </Menu.Item>
          <Menu.Item active={value === '2'} onClick={() => setValue('2')}>
            Item 2
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu id="kurator" title="Group 2" icon="kurator">
          <Menu.Item active={value === '3'} onClick={() => setValue('3')}>
            Item 3
          </Menu.Item>
          <Menu.Item active={value === '4'} onClick={() => setValue('4')}>
            Item 4
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu id="touch" title="Group 3" icon="touch">
          <Menu.Item active={value === '5'} onClick={() => setValue('5')}>
            Item 5
          </Menu.Item>
          <Menu.Item active={value === '6'} onClick={() => setValue('6')}>
            Item 6
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.Item id="tags" icon="tags" active={value === '7'} onClick={() => setValue('7')}>
          Item 7
        </Menu.Item>
        <Menu.Item
          icon="settings"
          active={value === '8'}
          onClick={() => setValue('8')}
        >
          Item 8
        </Menu.Item>
      </Menu>
    );

}}

</Playground>

### Disable togglable

<Playground>
  {() => {
    const [value, setValue] = React.useState('1');

    return (
      <Menu>
        <Menu.SubMenu
          id="understood"
          title="Understood"
          icon="understood"
          togglable={false}
          onClick={() => console.log('click understood')}
        >
          <Menu.Item active={value === 'intent'} onClick={() => setValue('intent')}>
            Intent
          </Menu.Item>
          <Menu.Item active={value === 'entity'} onClick={() => setValue('entity')}>
            Entity
          </Menu.Item>
        </Menu.SubMenu>
        <Menu.SubMenu id="kurator" title="Kurator" icon="kurator">
          <Menu.Item active={value === 'action'} onClick={() => setValue('action')}>
            Action
          </Menu.Item>
          <Menu.Item active={value === 'menu'} onClick={() => setValue('menu')}>
            Menu
          </Menu.Item>
        </Menu.SubMenu>
      </Menu>
    );

}}

</Playground>

## API

### Menu

<PropsTable of={Menu} />

### Menu.SubMenu

<PropsTable of={Menu.SubMenu} />

### Menu.Item

<PropsTable of={Menu.Item} />

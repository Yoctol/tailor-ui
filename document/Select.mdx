---
name: Select
menu: Components
route: /components/select
---

import { PropsTable, Playground, Link } from 'docz';
import { range } from 'ramda';
import { Select } from 'tailor-ui';

# Select

Select component to select value from options.

## When To Use

- A dropdown menu for displaying choices - an elegant alternative to the native `<select>` element.
- Utilizing <Link to="/components/radio">Radio</Link> is recommended when there are fewer total options (less than 5).

## Examples

```js
import { Select } from 'tailor-ui';
```

### Basic

<Playground>
  {() => {
    const [value, setValue] = React.useState({ label: 'Item 1', value: 1 });

    return (
      <Select
        value={value}
        onChange={setValue}
        options={[
          {
            label: 'Item 1',
            value: 1,
          },
          {
            label: 'Item 2',
            value: 2,
          },
          {
            label: 'Item 3',
            value: 3,
          },
          {
            label: 'Item 4',
            value: 4,
          },
          {
            label: 'Item 5',
            value: 5,
          },
        ]}
      />
    );

}}

</Playground>

### Creatable

<Playground>
  {() => {
    const [loading, setLoading] = React.useState(false);
    const [value, setValue] = React.useState({ label: 'Item 1', value: 'Item 1' });
    const [options, setOptions] = React.useState([
      {
        label: 'Item 1',
        value: 'Item 1',
      },
      {
        label: 'Item 2',
        value: 'Item 2',
      },
    ]);

    return (
      <Select
        creatable
        isLoading={loading}
        value={value}
        onChange={setValue}
        options={options}
        onCreateOption={name => {
          const newOption = { label: name, value: name };
          setLoading(true);
          setTimeout(() => {
            setOptions([...options, newOption]);
            setValue(newOption);
            setLoading(false);
          }, 3000)
        }
      }
      />
    );

}}

</Playground>

### isDisabled

<Playground>
  {() => {
    const [value, setValue] = React.useState({ label: 'Item 1', value: 1 });

    return (
      <Select
        isDisabled
        value={value}
        onChange={setValue}
        options={[
          {
            label: 'Item 1',
            value: 1,
          },
          {
            label: 'Item 2',
            value: 2,
          },
          {
            label: 'Item 3',
            value: 3,
          },
          {
            label: 'Item 4',
            value: 4,
          },
          {
            label: 'Item 5',
            value: 5,
          },
        ]}
      />
    );

}}

</Playground>

### With placeholder & isClearable

<Playground>
  {() => {
    const [value, setValue] = React.useState();

    return (
      <Select
        isClearable
        placeholder="どれ"
        value={value}
        onChange={setValue}
        options={[
          {
            label: 'Item 1',
            value: 1,
          },
          {
            label: 'Item 2',
            value: 2,
          },
          {
            label: 'Item 3',
            value: 3,
          },
          {
            label: 'Item 4',
            value: 4,
          },
          {
            label: 'Item 5',
            value: 5,
          },
        ]}
      />
    );

}}

</Playground>

### Default Multiple Selection

<Playground>
  {() => {
    const [value, setValue] = React.useState();

    return (
      <Select
        isMulti
        placeholder="choose the flags"
        value={value}
        onChange={setValue}
        options={[
          {
            label: 'apple',
            value: 'apple',
          },
          {
            label: 'banana',
            value: 'banana',
          },
          {
            label: 'guava',
            value: 'guava',
          },
        ]}
      />
    );

}}

</Playground>

### Multiple Selection with Text Style and No Delimiter

<Playground>
  {() => {
    const [value, setValue] = React.useState();

    return (
      <Select
        isMulti
        multiSelectMode="text"
        delimiter=""
        placeholder="choose the flags"
        value={value}
        onChange={setValue}
        options={[
          {
            label: 'i',
            value: 'i',
          },
          {
            label: 'g',
            value: 'g',
          },
          {
            label: 'm',
            value: 'm',
          },
          {
            label: 'u',
            value: 'u',
          },
          {
            label: 'y',
            value: 'y',
          },
        ]}
      />
    );

}}

</Playground>

## API

<PropsTable of={Select} />
